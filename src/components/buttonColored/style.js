import {StyleSheet} from 'react-native';
import {COLORS} from '../../utils/colors';

export default StyleSheet.create({
  container: {
    marginHorizontal: 20,
    paddingVertical: 15,
    alignItems: 'center',
    borderRadius: 50,
    borderWidth: 1,
  },
  text: {
    // color: COLORS.white,
    fontSize: 16,
  },
});
